# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets — never commit
.env
.streamlit/secrets.toml

# Data: dated datasets (data/yc_dataset_*.{parquet,xlsx}) and the AI cache
# (data/cache/ai_cache.json) ARE committed so the dashboard has data. Ignore only
# local/scratch bits:
data/user_data.csv
data/*.slug.bak
data/raw/
!data/.gitkeep
!data/cache/.gitkeep

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
